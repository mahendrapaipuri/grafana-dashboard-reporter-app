apiVersion: 1

apps:
  - type: mahendrapaipuri-dashboardreporter-app
    org_id: 1
    org_name: Main Org.
    disabled: false
    jsonData:
      appUrl: http://localhost:3000
      orientation: portrait
      layout: simple
      panels: default
      maxRenderWorkers: 2
      persistData: false

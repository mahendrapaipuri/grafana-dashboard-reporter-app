apiVersion: 1

apps:
  - type: dashboard-reporter-app
    org_id: 1
    org_name: Main Org.
    disabled: false
    jsonData:
      appUrl: http://localhost:3000
      orientation: portrait
      layout: simple
      maxRenderWorkers: 2
